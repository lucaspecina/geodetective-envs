# Changelog

## 2026-05-14

- **LLM adapter Anthropic + Claude vía Foundry funcionando** (commits 7e663ca, 3ee2916). Descubrimos via smoke test que (a) DeepSeek-V3.2 NO tiene visión — Foundry dropea silenciosamente `image_url` y el modelo alucina descripciones genéricas. Verificado sobre las 3 fotos del pilot E008: DeepSeek "vio" arquitectura soviética con Ladas en Tomsk 1898 mientras gpt-5.4/Kimi/grok describieron correctamente la panorámica B/N. El health_check minimal previo no detectaba el bug. (b) Claude en Foundry usa endpoint distinto (`/anthropic/v1/messages`) con header `Authorization: Bearer` y formato Anthropic Messages API nativo — incompatible con cliente OpenAI Chat Completions. Modelo correcto en catálogo es `claude-opus-4-6` (no `claude-opus-4-6-3` que era alias confuso). Nuevo `src/geodetective/llm_adapter.py`: MODEL_SPECS registry (21 modelos con provider/vision/tools), `complete()` routea por provider, OpenAI path = passthrough (no regresión), Anthropic path = httpx + format translation (system top-level, strict alternation, image base64 source, tool_use/tool_result blocks, input_schema). Bug crítico identificado por Codex review: la fusión de mensajes consecutivos same-role tenía bug — el branch `role=tool` no flusheaba pending_assistant y el final flush order era invertido. Fix aplicado. `react.py` ahora usa el adapter sin regresión (smoke test gpt-5.4 + claude-opus-4-6 ambos llegan a submit_answer). Refactor menor: thinking_blocks de Anthropic capturados como evento trace separado.
- **agentic_probe() en run_multimodel_pilot.py reemplaza el health_check minimal**. Valida text + vision (cuadrado rojo, expect "red") + tool calling + continuación post tool_result. 3 turns separados para evitar que el modelo priorice una capability sobre otra. Sobre los 9 modelos del set tiered: 9/9 pasan probe correctamente. DeepSeek-V3.2 correctamente rechazado por vision broken (saw "blue" instead of "red"). Models que tenían thinking mode (Kimi K2.5/K2.6) requieren `max_completion_tokens≥500` — el `=20` original consumía thinking sin dejar output. OpenAI rechaza `tool_choice` cuando no se pasan tools — adapter fix para condicional.
- **Process eval annotator (CORRAL-adapted) stub funcionando**. Nuevo módulo `src/geodetective/judge/` con: (a) `serialize_trace.py` traza ReAct → texto numerado [MSG N] con thinking blocks + tool args/results resumidos; (b) `prompts.py` STAGE1 (nodes H/T/E/J/U/C con grounding verbatim) + STAGE2 (edges con quotes); (c) `annotator.py` orquesta Stage 1+2 via llm_adapter + Stage 3a Python deterministic; (d) `pattern_matcher.py` detect_patterns_structural — 9 productive motifs y 8 reasoning breakdowns sobre estructura del grafo. Patterns normativos / counterfactuales (proxy_substitution, tool_channel_mismatch, geocoding_loop, etc.) deferidos a Stage 3b LLM-judge. `scripts/run_annotator.py` CLI con resume + filtros. Smoke test sobre 1 trace E005 v3 (Basel/gpt-5.4): **63 nodes / 54 edges / 7 productive motifs detectados / 3 breakdowns** (contradiction_without_repair, evidence_non_uptake, uninformative_test). Tiempo ~190s/trace con claude-opus-4-6 como judge. Mix realista de razonamiento bueno y roto — exactamente lo que CORRAL discriminaría.
- **Pilot E009 cross-modelo arrancado** (background): 9 modelos tiered (Top: gpt-5.4 / claude-opus-4-6 / grok-4.3 / Kimi-K2.6; Mid: gpt-5.4-mini / claude-sonnet-4-6 / grok-4-1-fast-reasoning / Kimi-K2.5; Ref: gpt-4o) × 3 fotos pilot (Tomsk, Dealey Plaza, Basel) × max_steps=30. Probe pasó los 9. Output a `experiments/E009_multimodel/`.

## 2026-05-12

- **Cleanup: `v3_thinking_visible` declarado canónico, v1/v2 deprecadas**. User clarificó post-commit anterior que v1_mechanical y v2_descriptive son variantes exploratorias del pilot que ya se descartaron — solo v3 vale de acá en adelante. Updates: (a) `process_eval_design.md` §5.2 simplificado, sacó toda la sección de "asimetría v1/v3" y las 3 opciones de resolución; (b) `findings_so_far.md` §1 reframed con números v3 (1 acierto Dealey 0 km, Tomsk perdido), §3 hipótesis abiertas actualizadas; (c) `CURRENT_STATE.md` reflejando v3 canónico + diseño process eval; (d) `README.md` paso 4 ahora pide explícitamente `PROMPT_VERSION=v3_thinking_visible`, paso 5 (antes "ablación") simplificado a "generar report v3", paso 6 mergeado. Nota histórica sobre v1/v2 deprecation en README + process_eval. Task #9 (patch react.py para thinking unificado) **eliminada** — ya no aplica. Implicación operativa: el annotator stub (task #6) puede arrancar directo sobre las 6 trazas v3 existentes; el cross-model run (task #7) usa solo v3.
- **3 docs nuevos en `research/synthesis/` para tracking paper-quality** + Codex review (1 ronda) aplicada. (a) `process_eval_design.md`: mapping del framework epistemológico CORRAL (Ríos-García et al. 2026) al dominio geo-detective — 6 nodos H/T/E/J/U/C + 6 edges + 7 productive motifs + 10+1 breakdowns adaptados con ejemplos reales del E005 (Tomsk acierto, Dealey max_steps, Molodechno proxy substitution). Diseño completo del annotator: pipeline multi-stage (Stage 1+2 nodos/edges con sliding window 20/15, Stage 3a graph-structural determinista, Stage 3b LLM-judge semantic con multimodal cuando aplica), validación con 15-20 traces estratificadas + 2 anotadores, costo $5-20 USD pilot / $300-1000 corpus escalado. Aportes genuinos al inventario CORRAL: 2 motifs (`language_pivot_productive`, `temporal_spatial_anchoring`) + 1 breakdown (`tool_channel_mismatch`); 4 subtipos/anti-motifs nombrados para diagnóstico (`proxy_substitution`, `geocoding_loop`, `language_monolingual_fixation`, `multi_modal_cross_validation`). Decisión canon: process eval es **offline only**, no entra al reward (alinea con PROJECT.md invariante 4 + recomendación CORRAL).
- (b) `related_work.md`: posicionamiento externo del proyecto vs landscape. Tabla retaxonomizada en 6 familias ortogonales (geoloc por outcome / reasoning chain / agentic con trazas / process-graph / datasets históricos / general agent). Verificados via WebFetch: **GTPred** (Li et al. 2026, 370 imgs / 120 años / reasoning chains annotated, no agentic) y **GeoBrowse** (2026, 9 tools agentic + expert-annotated stepwise traces). Reframing honesto post-Codex: ya no "primer benchmark histórico" sino "primer benchmark **agéntico** sobre foto histórica con anti-shortcut explícito + process eval estilo CORRAL". Sección §8 lista qué papers están leídos deep vs vía abstract vs solo en leverage_landscape — los segundos y terceros NO usables para framing fuerte hasta deep dive pendiente.
- (c) `findings_so_far.md`: síntesis transversal a través de E001-E005. Hallazgos por dimensión (corpus, tools, behavior, anti-shortcut, variancia). Hipótesis validadas/falseadas/abiertas explícitas. Falseadas notables del genesis: sesgo Rusia "70-95%" (real 62%), 47 shards PastVu (real 2094). Hipótesis abiertas con plan de testeo y costo: "el web-search bot es modelo o scaffold" (cross-model run, bloqueado por .env), "process score discrimina" (annotator), "max_steps=30 con prompt visual ayuda" (A/B test). Honesto sobre overclaims post-Codex: claims con n=1-6 reframeadas como "existe el efecto, magnitud requiere más samples".
- **Codex review aplicada (1 ronda) sobre los 3 docs**. Findings críticos corregidos: (1) mapping CORRAL era H/T/E/J/C/F → reconciliado a H/T/E/J/U/C con `submit_answer = C terminal` (no nodo nuevo); (2) evidencia visual primaria (foto target, crops) no entraba en el grafo → agregado como modalidad de E (`visual_primary`, `visual_crop`); (3) asimetría de logging v1/v2 (sin thinking) vs v3 (con thinking) invalidaba comparación cross-prompt del annotator → deuda explícita: patch `react.py` para capturar `msg.content` siempre como evento thinking, re-correr v1/v2 (task #9 creada); (4) Stage 3 determinista no alcanza para patterns normativos como `tool_channel_mismatch` → split en 3a (graph-structural Python) + 3b (LLM-judge semantic, multimodal cuando aplica); (5) GTPred missing del related work → agregado; (6) GeoBrowse mischaracterizado → corregido (sí tiene expert-annotated traces); (7) §6 process_eval sugería reward integration → contradice PROJECT.md, corregido a eval offline only; (8) overclaims con n=1-6 en findings → reframeados con caveats explícitos.
- **README.md reescrito** con enfoque benchmark (env como deuda futura) + paso a paso operativo del pipeline completo. 6 pasos canónicos: audit PastVu (#3) → sample diverso (#17) → atacker GPT-4o (#24) → ReAct pilot (#26) → ablación prompt v1/v2/v3 → reports HTML interactivos. Cada paso con comando, input/output, tiempo, variantes via env vars. Sección de pipelines alternativos (Test 3 baseline, ReAct E001 legacy). Tip de open de reports HTML en Windows.

## 2026-05-11

- **Ablación de prompt v1/v2/v3 sobre las 6 fotos del pilot E005** + frontend interactivo de trayectorias. Iteración exploratoria para responder por qué el agente no usaba `historical_query`, `static_map`, `street_view` (#26 hallazgo). 3 variantes de SYSTEM_PROMPT corridas sobre mismas 6 fotos / mismo modelo `gpt-5.4` / seed=42: **v1 mechanical** (descripciones puras de signature), **v2 descriptive** (agrega sección "Aporta" por tool — qué información devuelve, sin prescribir cuándo usarla), **v3 thinking_visible** (v2 + sección "Razonamiento visible" pidiendo verbalización observación+hipótesis+expectativa antes de cada turn, ReAct-style). **Hallazgos**: v2 activó tools diferenciales solo en 1 de 6 fotos (Basel: HQ=1, SM=2, SV=1), las otras 5 siguen en 0. v3 también casi nada (1 SM en Basel, 0 demás). v3 logró 0km en Dealey Plaza (perfect submit que v1/v2 fallaron). v3 PERDIÓ el acierto de Tomsk de v1 (2km → 3743km). **Conclusión preliminar**: el prompt descriptivo SÍ hace al modelo más consciente de las tools, pero no las pone en juego sistemáticamente. La verbalización ayuda en algunos casos y distrae en otros. Cambios en `react.py`: captura `msg.content` como evento `thinking`, top_results de web_search, snippet de fetch_url, imágenes base64 de image_search/fetch_url_with_images/static_map/street_view, top_results de geocode con coords. Nuevos scripts: `analyze_pilot_trajectories.py` (stdout viewer), `build_pilot_report.py` (HTML report por versión con selector de foto + mapa Leaflet + trayectoria step-by-step + tool descriptions panel + crops inline regenerados + imágenes/mapas embebidos), `build_compare_report.py` (HTML comparación cruzada con mapa central + 3 columnas v1/v2/v3), `compare_pilots.py` (comparador stdout). Run pilot script versionable con env `PROMPT_VERSION`. Artefactos en `experiments/E005_react_pilot/` exceptuados del gitignore para portabilidad entre máquinas.
- **#26 cerrado — Pilot E005 ReAct agent sobre corpus piloto**. Primera corrida end-to-end del agente ReAct (12 tools, `gpt-5.4`, max_steps=12) sobre 6 fotos del corpus piloto (1 por bucket país, seed=42). Nuevo script `scripts/run_react_pilot.py` que lee `experiments/E004_attacker_filter/results.json` con `decision=='keep'` y samplea balanceado. Tiempo: 994s. Resultados: 4/6 submitieron respuesta, 2/6 hit max_steps sin submit. **1 acierto preciso** (#2126812 Tomsk Russia-Asia 1898, 1.8 km del lugar real — el atacante había sido `baja/baja/baja` sin coords, tools le ganaron decisivamente). 3 respuestas off (301, 707, 1352 km). 2 N/A (Dealey Plaza USA con `target_match=1` confirmando que la deuda hash funcionó, y Mukden China 1946). **Patrón crítico detectado**: `historical_query`, `static_map`, `street_view` = **0 usos en las 6 fotos**. Web_search saturado (7-15 calls/foto). El agente actúa como web-search bot, no aprovecha tools visuales/históricas — incluso cuando tiene coords concretas (Tomsk). Iteración Codex (1 ronda review): NO escalar K_PER_CELL todavía, NO subir max_steps (compraría más web_search), proponer mini-ablation de prompt como próximo paso (mismo set, dos variantes: prompt actual vs con affordance explícito de tools visuales). Doc completo en `research/notes/E005_react_pilot.md`. Output: `experiments/E005_react_pilot/results.json` (gitignored).
- **#24 cerrado — atacante adversarial GPT-4o + deuda hash perceptual**. Cierra el epic #21 (pipeline de filtrado). Nuevo script `scripts/run_attacker_filter.py`: lee las 180 fotos de E007, baja+limpia cada una (vía `clean_image` de #22), corre N=3 llamadas a `gpt-4o` sin tools en paralelo (ThreadPoolExecutor, 8 workers), parsea lat/lon/confidence, computa distancia geodésica vs ground truth, y descarta si en ALGUNA corrida cumple `dist<10km AND conf in {media, alta}`. Cada result registra `triggering_runs` con índices de las corridas que dispararon (Codex review). Resultado: **101 keep (56.1%), 79 reject (43.9%), 0 skip**. 60/79 rejects (76%) dispararon las 3 corridas — patrón consistente, no ruido. Tiempo: 386s (6.4 min) con 8 workers. Distribución por bucket país: Norteamerica reject_rate 80% (most familiar para gpt-4o), Russia-Asia 13% (menos), Russia-EU 27%, Ex-URSS 37%, Europa-no-URSS 50%, Resto 57%. Por década: 1890s/1900s 53-57% (landmarks), 1930s 30% (más cotidianas). **Deuda hash perceptual implementada** en `src/geodetective/agents/react.py`: cuando `image_search` o `fetch_url_with_images` devuelven imagen con `is_likely_target=True`, ahora se OCULTA del modelo (hard reject): ni bytes ni URL — solo se informa cuántas se ocultaron. Alinea código con `PROJECT.md`. Iteración Codex (1 ronda): aplicados 3 puntos — (a) redacción de URL en `images_metadata` para target matches (no solo bytes; el dominio del match es shortcut), (b) `triggering_runs` para mejor signal en analysis, (c) lenguaje del audit moderado (Norteamerica 80% es "consistent with" familiaridad GPT-4o + landmark-prevalence, no causal sin control). Output completo: `research/notes/E004_attacker_filter.md`. Próximo: ya no hay sub-issues abiertas del epic #21, queda como issue futura escalar K_PER_CELL para corpus final balanceado.
- **#17 cerrado — sample diverso del corpus PastVu**. Nuevo script `scripts/sample_diverso.py`: lee el dump bajado en #3, filtra eligibles (type=1 + geo + year + 1890-1949) → 596K candidatos, asigna cada uno a celda `(bucket_pais, bucket_decada)` (6×6 = 36 celdas), de-duplica por geohash5 (~5km) dentro de cada celda y sortea K=5 fotos por celda con seed=42. Output: `experiments/E007_sample_diverso/candidates.json` (180 fotos, todas las celdas llenas) + `audit_summary.json`. Buckets país: `Russia-EU` (lon<60°), `Russia-Asia` (lon≥60°), `Ex-URSS` (14 países post-soviéticos), `Europa-no-URSS` (33 países incl. microestados), `Norteamerica` (USA/Canada), `Resto`. Décadas: 1890s-1940s. Iteración Codex (1 ronda review): aplicados 4 puntos — microestados europeos agregados a EUROPA_NO_URSS (Mónaco antes caía en Resto), schema notes para consumidores futuros (`file_url` no `file`, `provenance_source` no `source`), dedup intra-cell documentada (mismo geohash puede aparecer en distintas décadas, intencional), confirmación de determinismo del shuffle+seed. Doc en `research/notes/sample_diverso_audit.md`. Notable: sample incluye Wall Street 1895, Casino de Monte-Carlo 1894, Nanking Road Shanghai 1890 — landmarks famosos que el atacante de #24 debería detectar como trampeables.
- **#3 cerrado — audit de metadata PastVu**. Bajado `pastvu.jsonl.zst` (282 MB, HF `nyuuzyou/pastvu`) y streameado completo en 35s (`scripts/audit_pastvu_metadata.py`). Findings clave: 2.08M records (98% fotos type=1), 97% con `geo`, 99.95% con `year`, 100% con país (regions[0]). Sesgo Rusia: 62% (74% ex-URSS) — menos extremo que genesis-intro afirmaba (70-95%). **100% de las fotos tienen watermark** (`waterh > 0`) — `clean_image` (#22) confirmado como obligatorio. **676K fotos elegibles** (type=1 + geo + year + 1850-1950). 2094 shards de imágenes confirmados (no 47 como decía pastvu_deep_dive.md — esa snapshot estaba vieja). Doc completo en `research/notes/pastvu_metadata_audit.md`. Iteración Codex (1 ronda): refinamientos aplicados — 6 buckets de país (no 5, USA con masa propia), audit de campos extra (`regions` depth, `s`, `dir`, `year2`, dimensiones). **Cambio de decisión post-audit (user)**: urbano/rural soltado como dimensión de balance — no hay field directo y la derivación es frágil, país × década alcanza para v1. Updates a bodies de #21 y #17 reflejando esto.
- **Decisiones operativas del pipeline de filtrado (#21)** cerradas y documentadas en bodies de #21 (epic), #24 (atacante) y #17 (sample diverso): (a) "trampeable" = A (pistas escondidas) + B (visualmente obvia) + C (soluble por memoria), con B≈C en práctica (B se detecta porque cae en C); (b) modelo atacante = `gpt-4o` sin tools, 1 modelo en v1 (no multi-modelo, deuda documentada); (c) umbral = `dist_min<10km AND conf≥media` en N=3 corridas (mismo criterio que filtro v2 E001); (d) balance del sample = país/región × década × urbano/rural + de-clustering geográfico (geohash/H3) permanente; (e) hash perceptual match con target = hard reject (no flag) — alinear `react.py` con `PROJECT.md` queda como deuda explícita en #24. Mini threat model (5 filas: EXIF / watermark / source domain / reverse-search / modelo-de-memoria) agregado al body de #21 — cubre lo necesario para arrancar, #10 completo sigue abierto pero no bloquea. Orden de ejecución del pipeline: #3 → #17 → #24.

## 2026-05-08

- **Blacklist runtime per-photo** (#23): nuevo módulo `src/geodetective/corpus/blacklist.py` con `BLOCKED_DOMAINS_GLOBAL` (frozenset minimal: reverse search engines + agregadores con metadata estructurada + hosting/sharing) + `PROVIDER_DOMAINS` dict (pastvu, smapshot, etoretro, humus, oldnyc, oldsf, historypin, sepiatown) + `compute_excluded_domains(provider, source, extra)` + `is_blocked(url, excluded_domains)` con `urlparse` + suffix match (sin substring false positives). Las 4 tools del agente (`web_search`, `fetch_url`, `fetch_url_with_images`, `image_search`) aceptan `excluded_domains` y rechequean URL post-redirect. `web_search` overfetcha *3 a Tavily. ReAct loop computa `excluded_domains` una vez desde provider+source y lo pasa a cada tool call. SYSTEM_PROMPT actualizado para no enumerar dominios bloqueados. `sample_pastvu.py` agrega field `provider="pastvu"`. Tests sintéticos en `scripts/test_blacklist.py`: 14 grupos, 65 checks, cubren suffix match, GLOBAL, per-provider, source schemeless, E2E, anti-overblocking. Iteración Codex (2 rondas review): 8 bugs reales aplicados (substring → suffix, post-redirect, SYSTEM_PROMPT mintiendo, pre-filter `max_results`, `<img>` con final_url, google/bing/googleusercontent overblocking, schemeless URLs, back-compat semántica). **Cambio importante**: `BLOCKED_DOMAINS` de `web_search.py` ya NO contiene pastvu/smapshot/etc — pasa per-photo. Alias retenido para back-compat de imports.
- **Paso 0 del filtrado implementado** (#22, cierra #18): nuevo módulo `src/geodetective/corpus/clean_image.py` con `clean_image(raw_path, provider, provider_meta) -> CleanResult`. Hace strip EXIF + crop watermark por regla de provider + RGBA→RGB sobre fondo blanco + re-encode JPEG strippeando ICC/comments/XMP. Escritura atómica (temp + replace). Filename versionado `_clean_v{N}.jpg` para invalidar cache cuando cambia la lógica. Refactor de `scripts/test3_no_tools.py:download_photo()` y `scripts/run_react_websearch.py` para consumirlo. Tests sintéticos en `scripts/test_clean_image.py`: 13 escenarios (T1-T13) cubriendo EXIF, RGBA, P+transparency, ICC, JPEG comment, metadata patológica (waterh≥h), ceil clamp, cache. Iteración con Codex (1 ronda review): aplicados 5 issues reales — `int()` floor → `math.ceil()` + clamp, metadata patológica → `discarded` en vez de pass-through, modos `PA/La/RGBa` al composite blanco, write atómico con `os.replace()`, `img.info` se carrea en JPEG save → re-crear imagen desde pixels.
- **Diseño operativo del pipeline de filtrado del corpus** documentado como issue #21 (epic) + 3 sub-issues accionables. Decisiones canónicas: (a) atacante adversarial barato con modelo chico en 3 rondas (sin tools / con búsqueda / TinEye sampleado), no solo Test 3 estático; (b) Paso 0 de limpieza pre-filtro con strip EXIF + crop watermark por regla de fuente + RGBA→RGB (cierra #18, sub-issue #22); (c) blacklist de dominios runtime cambia de global a per-photo — el resto del archivo histórico queda accesible para investigación legítima (sub-issue #23); (d) Paso 2A multi-modelo en sample diverso antes de aplicar el filtro a escala, evita overfit del corpus a un solo modelo (sub-issue #24). Decisiones pendientes (modelo atacante exacto, threshold de confianza, buckets de estratificación) quedan en el body de #21 como checklist. Relacionado a #10 (threat model conceptual).

## 2026-05-07

- Bootstrap del proyecto (#1): repo + Project v2 #6 + set de docs raíz (README, PROJECT, CLAUDE, CURRENT_STATE, CHANGELOG, AUTORESEARCH) + estructura `research/` y `experiments/` + skills mínimas (`/test`, `/status`) + memoria del proyecto + documento semilla movido a `research/notes/genesis-intro.md`.
- Pasada de research sobre proyectos para apalancar: 4 notes en `research/notes/` (geobenchx_deep_dive, osm_mcp_deep_dive, pastvu_deep_dive, leverage_landscape) + síntesis canónica post-crítica Codex en `research/synthesis/related_work_decisions.md`. Hallazgo principal: **OpenHistoricalMap** (Overpass temporal, CC0) promovido a Tier 1.5 como tool histórica.
- `PROJECT.md` invariantes 1, 2 y 4 refinadas: filtrado adversarial aplica al corpus completo (training incluido); reward optimizable separado de penalizadores de proceso; LLM judge restringido a eval offline (anti reward-hacking).
- Evaluación de viabilidad técnica con Codex en `research/synthesis/viability_assessment.md`. Hallazgos críticos: (a) Google Maps TOS prohíbe explícitamente usar Maps Content para training/validating ML — fuerza re-arquitectura "stack primario open, Google opcional con user key + non-training default"; (b) reverse image search web-scale es el bloqueador más serio — ningún proveedor cubre Lens/Yandex a costo razonable, requiere filtrado tiered best-effort; (c) OpenHistoricalMap promovido a v1 (no v1.5).
- **Pivote a framing benchmark**: foco actual del proyecto = **benchmark de evaluación**, no env de training. La idea env queda como deuda futura. Disclaimer agregado al inicio de `PROJECT.md`. Invariante 7 (TOS) reformulada para distinguir uso benchmark (defendible) vs training (prohibido). Notas agregadas en `research/synthesis/{viability_assessment,related_work_decisions}.md` indicando el shift. Naming "GeoDetective Envs" → eventual "GeoDetective Benchmark" queda como deuda explícita.
- **Primer código operacional** (`src/geodetective/` + `scripts/`): conda env `geodetective` con Python 3.11, integración con Azure Foundry (gpt-4o/4.1/5/5.4), Tavily para web search, PIL/geopy/httpx. Todo bajo el .env compartido con arcagi3 (mismas credenciales).
- **Experimento E001** (`research/notes/E001_test3_no_tools_results.md`): Test 3 (VLM sin tools, N=3 runs) sobre 17 fotos PastVu. Resultado: filtro v2 simplificado (source + dist_min<10km AND conf≥media) deja sobrevivir ~53%. Sweet spot confirmado: fotos cotidianas sin landmark + rurales URSS/Cáucaso/Kazajstán + pre-1900.
- **Experimento E002** (`research/notes/E002_react_websearch_first_run.md`): primer ReAct + web_search filtrada sobre #1748874 (SP barrio anónimo). Resultado: **2573 km sin tools → 8.5 km con web_search ReAct (mejora 300x)**. Concepto del benchmark VALIDADO end-to-end.
- **Plan de validación revisado** (`research/synthesis/validation_plan.md`): 6 fases con gates falsables, post-crítica Codex (sin fase legal — postergada). Estimado: 12-15 semanas hasta v1 publicable.
- **Stack completo de 11 tools del agente** (`src/geodetective/tools/`): web_search (advanced), fetch_url, fetch_url_with_images (con imagehash), image_search (con hash perceptual flag), geocode + reverse_geocode (Nominatim), historical_query (OpenHistoricalMap Overpass — pieza diferencial), crop_image + crop_image_relative (PIL local), static_map (Google Static Maps), street_view (Google Street View Static).
- **ReAct loop expandido** (`src/geodetective/agents/react.py`): inyección de imágenes en messages cuando una tool devuelve visuales. Hash perceptual `phash` de la foto target con flag `is_likely_target` (no bloqueo, solo flag). Counters por tool en ReActResult. Max steps subido a 12.
- **Google Maps Platform integrado**: Maps Static + Street View Static APIs. Free tier $200/mes. Cuotas restrictivas configuradas (500/día Static, 200/día Street View). Cobertura SV confirmada en BA, Volga rural, SP barrio.
- **Experimento E003** (`research/notes/E003_react_full_tools.md`): batch parcial 9/15 runs (cancelado por costo Tavily) + test 1 run con stack completo. Hallazgos: (a) tools NO siempre ayudan — eso ES señal del benchmark; (b) modelo a veces ignora tools óptimas (no usó street_view en el caso Strahov→1493 km); (c) decisión: NO forzar uso de tools en system prompt (sesgaría benchmark).
- **Fixes Codex E003 aplicados** (commit `06b4a9a`): system prompt 100% descriptivo (no prescriptivo), `submit_answer` expandido (visual_clues, external_evidence, rejected_alternatives, verification_checks, uncertainty_reason), `historical_query` preset churches a 6 tags + `temporal_confidence` + `require_dated`, `street_view` modo `contact_sheet` 4 headings auto + pano_date + distance_to_pano_m, image dimensions inyectadas al prompt, blacklist 17→39 dominios.
- **Test técnico tools de Maps** confirma que `street_view` y `static_map` funcionan al 100% end-to-end. El modelo gpt-5.4 puede invocarlos perfectamente cuando se le pide explícitamente — la "no-utilización" en runs de geolocalización es **decisión del modelo**, no bug técnico. Esto es exactamente lo que el benchmark va a discriminar entre modelos.
- **Schema submit_answer expandido como rúbrica forzada** (sin sesgar): el modelo ahora estructura su razonamiento en visual_clues, external_evidence, rejected_alternatives, verification_checks, uncertainty_reason. Permite medir calibración, diversidad de hipótesis, profundidad investigativa, honestidad.
- **Idea registrada como issue #20**: `submit_tentative` con feedback iterativo (frío/caliente tipo Wordle). Postergada a v2 del benchmark.
